server:
  port: 20001
spring:
  application:
    # 应用名称 默认注册到nacos上的实例名
    name: discovery-service-provider
  cloud:
    nacos:
      discovery:
        # nacos注册中心的地址, 多个地址以逗号隔开
        server-addr: localhost:8848